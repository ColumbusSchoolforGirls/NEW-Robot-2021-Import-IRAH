/*----------------------------------------------------------------------------*/
/* Copyright (c) 2019 FIRST. All Rights Reserved.                             */
/* Open Source Software - may be modified and shared by FRC teams. The code   */
/* must be accompanied by the FIRST BSD license file in the root directory of */
/* the project.                                                               */
/*----------------------------------------------------------------------------*/

/*package frc.robot.commands;

import edu.wpi.first.wpilibj2.command.CommandBase;
import frc.robot.subsystems.Flap;

public class FlapManual extends CommandBase {
  /**
   * Creates a new FlapManual.
   */
  /*private Flap m_flap;
  private boolean isOpen;
  private boolean auto = false;

  public FlapManual(Flap flap, boolean isOpen, boolean auto) {
    // Use addRequirements() here to declare subsystem dependencies.
    m_flap = flap;
    addRequirements(m_flap);
    this.isOpen = isOpen;
    this.auto = auto;
  }

  // Called when the command is initially scheduled.
  @Override
  public void initialize() {
    // put this in initialize so tht it is not continually running because it never finished because is finished
    // always returns false
    // putting it in initialize allows it to run once
      m_flap.Up(isOpen);
      if(auto){
        auto = true;
      }
    
  }

  // Called every time the scheduler runs while the command is scheduled.
  @Override
  public void execute() {
    
  }

  // Called once the command ends or is interrupted.
  @Override
  public void end(boolean interrupted) {
    auto = false;
  }

  // Returns true when the command should end.
  @Override
  public boolean isFinished() {
    return auto;
  }
}*/
